export type createApply = {
    user_id: number
    apply_bio: string
    apply_speacial: string
    apply_age: number
    apply_address: string
}

export type applyId = {
    id: number
}

export type updateApplyStatus = {
    status: string
}